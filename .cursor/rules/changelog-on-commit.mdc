---
description: 提交时更新 CHANGELOG，并默认执行 commit+push
alwaysApply: true
---

# 提交时同步更新 CHANGELOG（默认 commit + push）

当用户要求**提交代码**（如“提交下代码”）或你准备创建提交时，遵循以下默认流程：

1. 先更新 [`docs/CHANGELOG.md`](docs/CHANGELOG.md)；
2. 完成 `git commit`；
3. 在用户未明确禁止的前提下，默认继续执行 `git push`。

若用户明确说明“只 commit 不 push”，则按用户指令覆盖默认行为。

## 必须做的事

1. **先写 changelog，再 commit** — 不得在未更新 `docs/CHANGELOG.md` 的情况下单独提交功能/修复代码。
2. **按 commit 次数归档** — 使用 `## Commit N` 作为区块标题（`N` 为累计提交序号）；若本次提交对应序号区块已存在，在该区块内追加条目，不重复新建。
3. **按主题分组** — 使用 `### 分类名`（如「日志与配置」「核心模块」「文档」「部署」），每条用 `-` 列表，写清**做了什么**与**影响范围**（关键路径/模块名即可）。
4. **与 diff 一致** — 条目须反映本次 `git diff` 的真实变更；不要写未改动的内容。
5. **同一次提交** — `git add` 时包含 `docs/CHANGELOG.md` 与相关代码文件。
6. **默认推送远端** — commit 成功后默认执行 push，并在回复中告知分支与提交哈希。

## 不必记录的内容

- 仅本地生成、且已在 `.gitignore` 中的文件（如 `logs/`、`.venv/`、`.env`）
- 纯格式化、无行为变化的改动（除非用户明确要求记录）

## 提交信息

commit message 可简要概括技术变更；详细说明以 `docs/CHANGELOG.md` 为准。

## 示例（按 commit 次数归档）

```markdown
## Commit 12

### API

- `app/api/server.py`：新增健康检查路由 `/health`
```

若 `Commit 12` 已存在，则在该区块的 `###` 小节下追加 bullet，或新增合适的 `###` 小节。
