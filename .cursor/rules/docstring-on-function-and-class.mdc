---
description: 新增或修改函数/类时必须补充注释说明功能
alwaysApply: true
---

# 函数与类注释规范

当你新增或修改**函数**、**方法**、**类**时，必须添加或更新注释，清楚说明其功能与用途。

## 必须做的事

1. **函数/方法必须有说明**：至少写明“做什么”，复杂逻辑需补充关键步骤或边界条件。
2. **类必须有说明**：写清职责、核心用途，以及与上下游对象的关系（如状态对象、配置对象、存储对象）。
3. **参数与返回值要可理解**：若函数签名不够直观，注释中补充关键参数含义与返回结果。
4. **与代码保持一致**：修改实现后，同步更新注释，避免注释过时。
5. **避免空洞注释**：禁止仅写“处理数据”“执行逻辑”这类无信息描述。

## 推荐风格

- 优先简洁中文注释，1-3 行说清核心功能。
- 复杂流程可在函数体关键位置加少量行内注释，解释“为什么这样做”。
- 可读性优先，避免重复函数名本身已表达的信息。

## 示例

```python
def reload_settings() -> Config:
    """清除缓存并重新加载配置，用于测试或运行时刷新。"""
```

